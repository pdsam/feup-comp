import Package;
import Package(int, boolean);
import static Package.someAttr;
import static Package.statMethod(void) int;

class IfTest {
    boolean enterIf;

    public static void main(String[] args) {

    }

    public int[] ident(int param) {
        int a;
        int b;
        int c;

        enterIf = false;
        a = 0-5;

        if(enterIf) {
            //c = a;
        } else {
            c = param;
        }

        while(a < 1) {

            if(enterIf) {
                c = a;
            } else {
                b = a + c;
                c = b;
            }
            b = c;
            a = c;
        }

        a = b;
        return c;
    }
}