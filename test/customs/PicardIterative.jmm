public class Main {

    public static void main(String[] args)
    {
        int start_value;
        int end_value;
        int allowed_error;
        int temp;
        int count;

        int[] y1;
        int[] y2;
        int[] y3;

        start_value = 0;
        end_value = 30;
        allowed_error = 4;
        temp = start_value;
        count = 0;

        y1 = new int[30];
        y2 = new int[30];
        y3 = new int[30];

        while (temp <= end_value) {

            y1[count] = 1 + temp + (temp * temp) / 2;
            y2[count] = 1 + temp + (temp * temp) / 2 + (temp * temp * temp) / 3 + (temp * temp * temp * temp )/ 8;
            y3[count] = 1 + (temp) + (temp * temp) / 2 + (temp * temp * temp) / 3 + (temp * temp * temp * temp ) / 8 + (temp * temp * temp * temp * temp ) / 15 + (temp * temp * temp * temp * temp * temp) / 48;

            count++;
            temp = temp + allowed_error;
        }

     //   System.out.printf("\nX\n");

        temp = start_value;

        while (temp <= end_value) {
         //   System.out.print( temp);
            temp = temp + allowed_error;
        }

      //  System.out.printf("\n\nY(1)\n");
        for (temp = start_value, count = 0;
             temp <= end_value;
             temp = temp + allowed_error, count++) {

         //   System.out.print(y1[count]);
        }

        // System.out.printf("\n\nY(2)\n");

        temp = start_value;
        count = 0;

        while (temp <= end_value ) {

          //  System.out.print(y2[count]);

            temp = temp + allowed_error;
            count++;
        }

      //  System.out.printf("\n\nY(3)\n");

        temp = start_value;
        count = 0;

        while ( temp <= end_value ) {

        //    System.out.print(y3[count]);
            temp = temp + allowed_error;
            count++;
        }


    }
}
